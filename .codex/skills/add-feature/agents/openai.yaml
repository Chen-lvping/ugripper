interface:
  display_name: "Add Feature"
  short_description: "先规划后实现，并在收尾决策主包/Updater版本号"
  default_prompt: "Use $add-feature to plan first, implement after confirmation, sync docs/agent updates, then decide whether to bump build_deb.sh/usb_updater_build.sh versions based on user instruction or change complexity."
