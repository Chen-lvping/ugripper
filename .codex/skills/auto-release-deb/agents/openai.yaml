interface:
  display_name: "Auto Release Deb"
  short_description: "确认发布影响范围并判断 ugripper/updater 构建策略"
  default_prompt: "Use $auto-release-deb after code changes to analyze impacted packages and decide whether ugripper needs full build or quick build, and whether updater needs full build. Do not bump versions in this skill."
