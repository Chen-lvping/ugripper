#!/bin/bash
set -e

SERVICE_NAME="{{APP_NAME}}.service"

# 停止并禁用业务服务
systemctl stop "$SERVICE_NAME" || true
systemctl disable "$SERVICE_NAME" || true

# 卸载时如果包会移除 udev 规则文件，可在这里 reload（即使文件还没删也无害）
udevadm control --reload-rules || true

exit 0
